






#include<iostream>
using namespace std;

int main()
{
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)cin>>a[i];
    int b[n];
    for(int i=0;i<n;i++)
    {
        if(i==0)b[i]=a[i];
        else
            b[i]=max(b[i-1]+a[i],a[i]);
    }
    cout<<b[n-1]<<endl;
}
